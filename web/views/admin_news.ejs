<%- include('partials/header') %>

<section class="card">
  <div class="row-between">
    <h2>Notícias</h2>
    <div class="admin-links">
      <a class="button-link" href="/admin/accounts">Contas</a>
      <% if (editorEnabled) { %>
        <a class="button-link" href="/admin/server-editor">Editor servidor</a>
      <% } %>
      <a class="button-link" href="/admin/news/new">Nova notícia</a>
      <a class="button-link" href="/admin/downloads">Downloads</a>
      <a class="button-link" href="/admin/logout">Sair</a>
    </div>
  </div>
  <% if (news.length === 0) { %>
    <p class="muted">Não há notícias.</p>
  <% } else { %>
    <table class="table">
      <thead>
        <tr>
          <th>ID</th>
          <th>Título</th>
          <th>Status</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% news.forEach(item => { %>
          <tr>
            <td><%= item.id %></td>
            <td><%= item.title %></td>
            <td><%= item.published ? 'Publicado' : 'Oculto' %></td>
            <td class="actions">
              <a href="/admin/news/<%= item.id %>/edit">Editar</a>
              <form method="post" action="/admin/news/<%= item.id %>/delete">
                <button type="submit" class="link-button">Excluir</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</section>

<%- include('partials/footer') %>


